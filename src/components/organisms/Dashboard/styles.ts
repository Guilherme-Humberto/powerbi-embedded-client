import { IReportDimensions } from '@interfaces/index'
import styled from 'styled-components'

export const Container = styled.div``

export const EmbedContainer = styled.div<{ dimensions: IReportDimensions }>``
